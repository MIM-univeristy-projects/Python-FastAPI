[project]
name = "python-fastapi"
version = "0.1.0"
description = "FastAPI for the 'Academic Neighbour' project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.119.1",
    "psycopg2>=2.9.11",
    "sqlmodel>=0.0.27",
]
